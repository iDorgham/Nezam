#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
python3 "$repo_root/.nezam/core/scripts/context/update-context-docs.py" >/dev/null 2>&1 || true
bash "$repo_root/.nezam/core/scripts/context/auto-memory-hooks.sh" --post-commit >/dev/null 2>&1 || true
