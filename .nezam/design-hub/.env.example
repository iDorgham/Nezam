# Design Hub — environment variables (local / deployment)

# Figma token sync (Phase 14 — POST /api/figma/sync)
# Requires Figma Enterprise + file_variables:write on your personal access token.
FIGMA_ACCESS_TOKEN=
FIGMA_FILE_KEY=
