# generated by core/scripts/sync/sync-ai-folders.js
name = "design-hub"
description = "design-hub"
prompt = """
# /design-hub — NEZAM Design Hub operator

Operate the local Design Hub app (sitemap, tokens, wireframes, lock export).

## Subcommands

| Subcommand | Action |
|------------|--------|
| `start` | Run Design Hub dev server from repo root: `pnpm design-hub` |
| `architecture` | Focus Architecture IA: apps, menus, pages, micro-services rack |
| `components` | Focus Components tab + shadcn registry |
| `wireframe` | Same as `/wireframe` — lock gate for P0 pages |
| `verify` | In `.nezam/design-hub/`: `pnpm test && pnpm build` |

## Paths

Resolve via `.nezam/workspace.paths.yaml` — do not hardcode.

## Agents

- `design-hub-architecture` — sitemap + services rack
- `design-hub-components` — components tab
- `design-hub-wireframe` — wireframe sessions + lock
- `design-hub-tokens` — design tokens
- `design-hub-specialist` — cross-cutting hub work
"""
