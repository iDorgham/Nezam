# Required Intake Folder

This folder is intentionally kept minimal and is used as the hardlock intake surface.

To unlock planning/development gates, place only:

- `PRD.md` (fully prepared, non-template)
- `PROJECT_PROMPT.md` (project-specific, non-template)

Legacy/seed artifacts were moved to:

- `docs/reference/`
