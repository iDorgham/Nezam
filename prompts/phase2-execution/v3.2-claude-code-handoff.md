# NEZAM v3.2 Hardening — Claude Code / Antigravity Handoff

**Use in:** Claude Code CLI | Antigravity IDE  
**Purpose:** Execute v3.2 phases with AI orchestration  
**Status:** Ready to fork as independent agent

---

## Overview

This document provides Claude Code and Antigravity with everything needed to execute NEZAM v3.2 hardening autonomously. Each phase is a standalone **prompt + execution context** that can be triggered via CLI, IDE terminal, or git hook.

---

## Phase 1: Foundation Hardening [DevOps]

### Prompt for Claude Code

```bash
claude-code --task "NEZAM v3.2 Phase 1: Foundation Hardening"
```

### Full Handoff Context

**Objective:** Clean sync drift, remove orphaned skills, enforce pre-commit hooks

**Input Files:**
- `.cursor/state/agent-status.yaml` (current)
- `.cursor/skills/` (all skill directories)
- `pnpm-lock.yaml` (if exists)

**Tasks (8 hours total):**

```
1. Sync Analysis (30 min)
   □ Run: pnpm ai:sync
   □ Run: pnpm ai:check
   □ Capture drift report to .nezam/core/reports/SYNC_ANALYSIS_v3.2.md
   
2. Orphaned Skills Cleanup (1.5 hours)
   □ Extract list of 25 orphaned skills from registry
   □ For each orphaned skill:
     - Document why orphaned (refactored/deprecated/duplicate)
     - Move to .cursor/skills/archive/[reason]/
     - Update CHANGELOG with removal
   □ Update .cursor/state/AGENT_REGISTRY.yaml (remove entries)
   □ Run: pnpm ai:check to verify

3. YAML Validation (30 min)
   □ Run: pnpm verify:yaml (if exists; if not, create script)
   □ Fix all parse errors in:
     - .cursor/state/plan_progress.yaml
     - .cursor/state/agent-status.yaml
     - .cursor/state/agent-bus.yaml
   □ Document fixes in .nezam/core/reports/YAML_FIXES_v3.2.md

4. Agent Status Schema Update (1 hour)
   □ Add to .cursor/state/agent-status.yaml:
     - last_sync: [timestamp]
     - certified_agents: [list]
     - versioning: [semver]
     - sync_drift_threshold: 0.005 (0.5%)
     - drift_check_interval: "weekly"
   □ Validate against schema in .cursor/state/schemas/

5. Weekly Drift Detection Job (1 hour)
   □ Create .github/workflows/sync-drift-check.yml:
     - Trigger: Weekly (Mondays 10am UTC)
     - Run: pnpm ai:check
     - Report: Drift %, orphaned count, YAML errors
     - Alert: Slack if drift > 1%
   □ Test workflow trigger
   □ Verify Slack webhook configured

6. Sync Runbook (1 hour)
   □ Create .nezam/core/docs/SYNC_RUNBOOK.md:
     - When to run pnpm ai:sync (on every commit)
     - What pnpm ai:check reports (5 metrics)
     - How to fix common drift issues
     - Recovery steps for state file corruption
     - Escalation procedure

7. Husky Pre-Commit Setup (30 min)
   □ Verify husky is installed: npx husky install
   □ Update .husky/pre-commit to run pnpm ai:sync
   □ Test locally: make change → commit → verify sync runs
   □ Document setup for new team members in CONTRIBUTING.md

Output Files:
- .nezam/core/reports/SYNC_ANALYSIS_v3.2.md
- .nezam/core/reports/YAML_FIXES_v3.2.md
- .nezam/core/docs/SYNC_RUNBOOK.md
- .cursor/state/agent-status.yaml (updated)
- .cursor/state/AGENT_REGISTRY.yaml (cleaned)
- .github/workflows/sync-drift-check.yml
- CONTRIBUTING.md (updated)

Success Criteria:
✅ Zero YAML parse errors
✅ 0 orphaned skills in registry
✅ Sync drift < 0.5%
✅ Pre-commit hook working on local commits
✅ Weekly drift check job scheduled + tested
✅ Sync runbook written + team-tested
```

---

## Phase 2: CI/CD Pipeline [DevOps + SRE]

### Prompt for Claude Code

```bash
claude-code --task "NEZAM v3.2 Phase 2: CI/CD Pipeline Full Validation"
```

### Full Handoff Context

**Objective:** Test CI pipeline end-to-end; validate all gates; document failures & recovery

**Input Files:**
- `.github/workflows/` (all workflows)
- `.nezam/core/specs/TECH_STACK.md`
- `package.json` (or equivalent build config)

**Tasks (20 hours total):**

```
1. Full CI Pipeline Test (2 hours)
   □ Create branch: git checkout -b test/ci-full-validation-v3.2
   □ Make test changes for each gate:
     - Lint: Add code style violation
     - Type: Add TypeScript error
     - Security: Add hardcoded secret
     - Perf: Add large bundle
     - Tests: Add failing test
   □ Commit & push to trigger CI
   □ Monitor each gate:
     - ESLint gate (should fail, then pass after fix)
     - TypeScript gate (should fail, then pass after fix)
     - CodeQL gate (should fail on secret)
     - Performance gate (should fail on bundle size)
     - Jest gate (should fail on test)
   □ Fix each violation and verify gate passes
   □ Document: Which gates caught which issue

2. All 7 Design Gates Validation (3 hours)
   □ Identify all 7 design gates from .cursor/rules/design-gates.mdc:
     1. Token usage (no hardcoded colors)
     2. Motion budget (animation performance)
     3. Performance thresholds (LCP, FID, CLS)
     4. Accessibility (WCAG AA)
     5. RTL layout (Arabic support)
     6. Design system consistency
     7. Responsive breakpoints
   □ For each gate:
     - Verify it runs in CI
     - Document what it checks
     - Test failure scenario
     - Document how to fix
   □ Create .nezam/core/reports/DESIGN_GATES_VALIDATION_v3.2.md

3. Release Workflow Test (2 hours)
   □ Trigger release job on test branch:
     - Version bump
     - Tag creation
     - Artifact generation (if applicable)
     - Deployment to staging (if applicable)
   □ Verify each step:
     - [ ] Version updated correctly
     - [ ] Git tag created
     - [ ] Release notes generated
     - [ ] Artifacts available
     - [ ] Staging deployment successful
   □ Document: Release workflow happy path

4. CI Failure Modes & Recovery (2 hours)
   □ Create .nezam/core/docs/CI_FAILURE_GUIDE.md:
     - For each gate: failure scenarios + recovery steps
     - How to force-merge if needed (approval process)
     - How to rollback a bad release
     - Slack alerts setup (5 min response time)
     - Escalation tree (who to contact)
   □ Document at least 10 failure scenarios:
     - ESLint failures
     - Type checking failures
     - Security scan findings
     - Performance budget breach
     - Test failures
     - Deployment failures
     - Artifact generation failures
     - Tag creation failures
     - Branch protection violations
     - Network timeouts

5. Rollback Procedures (2 hours)
   □ Document rollback for each stage:
     - Stage 1: Code rollback (git revert)
     - Stage 2: Tag rollback (git tag -d)
     - Stage 3: Staging rollback
     - Stage 4: Production rollback
   □ Test rollback workflow on test branch
   □ Create .nezam/core/docs/ROLLBACK_RUNBOOK.md:
     - Step-by-step for each stage
     - Time estimates
     - Who to notify
     - Communication template

6. CI Monitoring Dashboard Setup (1 hour)
   □ Set up GitHub Actions dashboard:
     - Recent runs (status, duration)
     - Failed gates (details, logs)
     - Success rate (7-day, 30-day)
   □ Set up Slack integration:
     - CI failure alerts (red)
     - Release success alerts (green)
     - Weekly health report
   □ Document dashboard access in runbook

7. Performance Budget Thresholds (2 hours)
   □ Define thresholds in .github/workflows/:
     - LCP (Largest Contentful Paint): < 2.5s
     - FID (First Input Delay): < 100ms
     - CLS (Cumulative Layout Shift): < 0.1
     - Bundle size: < 150KB gzipped
     - Test coverage: > 80%
   □ Configure lighthouse-ci in CI pipeline
   □ Set per-page budgets
   □ Test on sample pages
   □ Document thresholds in .nezam/core/reports/PERF_BUDGET_v3.2.md

8. Team Communication & Runbooks (2 hours)
   □ Create .nezam/core/docs/CI_RUNBOOK.md:
     - How to read CI logs
     - Common fixes for each gate
     - How to debug locally vs. in CI
     - When to ask for help
   □ Record 5-min video: "How to fix a failing CI build"
   □ Create Slack thread template for CI failures
   □ Train one team member (1 hour)

Output Files:
- .nezam/core/docs/CI_FAILURE_GUIDE.md
- .nezam/core/docs/ROLLBACK_RUNBOOK.md
- .nezam/core/docs/CI_RUNBOOK.md
- .nezam/core/reports/DESIGN_GATES_VALIDATION_v3.2.md
- .nezam/core/reports/PERF_BUDGET_v3.2.md
- .github/workflows/ci-health-check.yml
- .github/workflows/release.yml (refined)

Success Criteria:
✅ Full CI pipeline tested end-to-end
✅ All 7 gates validated + documented
✅ Release workflow tested + documented
✅ 10+ failure scenarios documented
✅ Rollback procedures tested
✅ Monitoring dashboard live
✅ Team can fix CI failures without help
```

---

## Phase 3: Security & Compliance [Security Officer]

### Prompt for Claude Code

```bash
claude-code --task "NEZAM v3.2 Phase 3: Security & Compliance Automation"
```

### Full Handoff Context

**Objective:** Enable CodeQL, DependaBot, secret scanning; run security audit; document baseline

**Input Files:**
- `.github/` (all config)
- `package.json` (dependencies)
- Code to analyze (any)

**Tasks (10 hours total):**

```
1. GitHub CodeQL Setup (1 hour)
   □ Enable CodeQL in GitHub Settings:
     - Settings > Code security > CodeQL > Enable
     - Choose language(s): JavaScript/TypeScript, Python, etc.
     - Configure custom queries if needed
   □ Test on test branch:
     - Introduce security issue (SQL injection, XSS, etc.)
     - Verify CodeQL catches it
     - Fix issue, verify gate clears
   □ Document: How to read CodeQL reports

2. GitHub DependaBot Setup (1 hour)
   □ Create .github/dependabot.yml:
     - package-ecosystem: "npm"
     - schedule: weekly
     - open-pull-requests-limit: 5
     - allow: ["dependency-type:all"]
   □ Enable DependaBot in Settings:
     - Settings > Dependability alerts > Enable
     - Auto-merge for minor/patch (optional)
   □ Test: Create a PR to verify bot works

3. GitHub Secret Scanning (30 min)
   □ Enable secret scanning:
     - Settings > Code security > Secret scanning > Enable
     - Push Key Scanning (if Enterprise)
   □ Test with fake secret:
     - Add to test branch: FAKE_KEY="sk-test-1234567890"
     - Commit & push
     - Verify GitHub detects it
     - Remove secret, verify detection
   □ Document: How to handle found secrets

4. Initial Security Audit (2 hours)
   □ Run /SCAN security in Cursor (if available)
   □ Or run: npm audit, npm audit fix
   □ Or run: CodeQL analysis on full codebase
   □ Document all findings in .nezam/core/reports/SECURITY_AUDIT_v3.2.md:
     - Category (critical, high, medium, low)
     - Description
     - Affected component
     - Remediation steps
     - Timeline (urgent, this sprint, backlog)
   □ Create remediation plan:
     - Urgent items: Fix this week
     - Sprint items: Fix this month
     - Backlog items: Track for v3.3

5. Input Validation Enforcement (2 hours)
   □ Review ADR-0001 (Auth0) for validation patterns
   □ Create .nezam/core/docs/SECURITY_GUIDELINES.md:
     - Input validation checklist
     - Output encoding rules
     - Authentication patterns
     - Authorization patterns
   □ Add to Feature Spec template:
     - Security section (3 questions)
     - Input validation requirements
     - Authorization rules
   □ Document in every new spec

6. Secrets Rotation Schedule (1 hour)
   □ Create .nezam/core/docs/SECRETS_ROTATION.md:
     - Auth0 keys: rotate quarterly
     - GitHub tokens: rotate annually or on team change
     - API keys: as-needed basis
     - Database credentials: monthly
   □ Set calendar reminders
   □ Document procedure for each secret type

7. Security Runbook (1.5 hours)
   □ Create .nezam/core/docs/SECURITY_RUNBOOK.md:
     - Incident response procedures:
       - Step 1: Identify (logs, alerts, reports)
       - Step 2: Contain (isolate, revoke, rotate)
       - Step 3: Remediate (patch, update, fix)
       - Step 4: Communicate (team, users, leadership)
     - Breach notification process:
       - Who to notify (legal, security, leadership)
       - Timeline (24 hours, per law)
       - Template message
     - Recovery steps:
       - Rollback procedures
       - Secret rotation
       - Monitoring increase
     - Escalation contacts:
       - Security team lead
       - Engineering lead
       - Founder/CEO

8. Security Baseline Documentation (1 hour)
   □ Create .nezam/core/meta/SECURITY_BASELINE.md:
     - CodeQL findings (count, categories)
     - DependaBot status (dependencies, outdated count)
     - Secret scan results (0 found)
     - Audit score (if applicable)
     - Known vulnerabilities (0)
     - Certification status

Output Files:
- .github/dependabot.yml
- .nezam/core/reports/SECURITY_AUDIT_v3.2.md
- .nezam/core/docs/SECURITY_GUIDELINES.md
- .nezam/core/docs/SECRETS_ROTATION.md
- .nezam/core/docs/SECURITY_RUNBOOK.md
- .nezam/core/meta/SECURITY_BASELINE.md

Success Criteria:
✅ CodeQL enabled + tested
✅ DependaBot configured + tested
✅ Secret scanning enabled
✅ Security audit complete + documented
✅ Remediation plan created
✅ Security runbook written
✅ Team trained on security culture
```

---

## Phase 4: Design System & Wireframe Bridge [Design Lead + Frontend Lead]

### Prompt for Claude Code

```bash
claude-code --task "NEZAM v3.2 Phase 4: Wireframe Server Integration"
```

### Full Handoff Context

**Objective:** Authenticate Figma MCP; test lock→unlock cycle; integrate with design workflow

**Input Files:**
- `.nezam/templates/wireframe-server/` (all schemas)
- `.cursor/` (design-related configs)

**Tasks (16 hours total):**

```
1. Figma MCP Authentication (30 min)
   □ Generate FIGMA_ACCESS_TOKEN from Figma account
   □ Store in .env (NOT in git):
     FIGMA_ACCESS_TOKEN=<your-token>
   □ Add to .env.example:
     FIGMA_ACCESS_TOKEN=<ask-devops>
   □ Add to GitHub Actions secrets (for CI)
   □ Verify token works: call Figma API test endpoint

2. Full /Wireframe Command Test (2 hours)
   □ Run /wireframe command in Cursor (if available)
   □ Generate project_context.json from:
     - PRD (existing or test PRD)
     - IA/menus (existing or test)
     - DESIGN.md (existing design profile)
   □ Verify project_context.json contains:
     - meta (project name, product type)
     - canvas_mode (web-marketing, saas-dashboard, etc.)
     - sitemap (page hierarchy)
     - pages (detailed page specs)
     - design_system (tokens, components)
     - ai_notes (instructions)
   □ Start wireframe server (if applicable)
   □ Verify connection to Figma

3. Lock→Unlock Cycle Testing (2 hours)
   □ Complete wireframe session in server
   □ Generate wireframes_locked.json
   □ Verify file contains:
     - design_decisions (rationale)
     - locked_components (exact props, no flexibility)
     - flexible_components (ranges, options)
     - content_slots (where content goes)
     - navigation_map (page flows)
     - implementation_contract (developer notes)
   □ Test unlock:
     - Edit wireframes in Figma
     - Re-lock and verify changes persist
   □ Repeat on 3 different project types:
     - [ ] Website (marketing)
     - [ ] Web app (dashboard)
     - [ ] SaaS product
   □ Document findings in .nezam/core/reports/WIREFRAME_TESTING_v3.2.md

4. Block Registry Audit (1 hour)
   □ Review .nezam/templates/wireframe-server/block_registry.json
   □ Verify all blocks have:
     - [ ] props defined (no empty properties)
     - [ ] defaults set
     - [ ] canvas_mode_defaults for each mode
   □ Fix any empty blocks
   □ Run validation script (create if needed):
     - node .nezam/scripts/validate-block-registry.js
   □ Ensure zero validation errors

5. Wireframe Runbook Creation (1.5 hours)
   □ Create .nezam/core/docs/WIREFRAME_RUNBOOK.md:
     - How to start a wireframe session
     - What project_context.json should contain
     - How to read lock output
     - How to debug lock issues:
       - Empty blocks error
       - Missing navigation
       - Incomplete content slots
     - How to recover from failures:
       - Revert to previous lock
       - Force unlock and re-lock
       - Manual block definition
     - Expected output structure
     - Escalation procedure

6. SCAFFOLD Gate Integration (1 hour)
   □ Update .cursor/rules/sdd-pipeline-v2.mdc:
     - SCAFFOLD gate now requires wireframes_locked.json
     - Gate validation:
       - File exists?
       - Valid JSON?
       - All required sections present?
   □ Create gate check script:
     - node .nezam/scripts/validate-wireframe-lock.js
     - Runs before SCAFFOLD unlock
   □ Test gate:
     - Try to unlock SCAFFOLD without lock file → FAIL
     - Provide lock file → PASS

7. Design-to-Code Workflow Documentation (1.5 hours)
   □ Create .nezam/core/docs/DESIGN_TO_CODE.md:
     - From locked wireframes → developer tasks:
       - How to extract components from lock
       - How to map locked props to React props
       - How to handle flexible components
       - How to build responsive layouts from specs
     - Component naming conventions
     - Responsive breakpoint mapping
     - Animation/motion implementation
     - Example: Take a locked wireframe, extract components, build component code
   □ Create template component file:
     - .nezam/templates/components/WireframeExtract.tsx (example)

8. Design Team Workflow Test (1 hour)
   □ Run session with designer:
     - Designer creates mock in Figma
     - Run /wireframe command
     - Designer reviews locked output
     - Designer iterates if needed
   □ Get feedback:
     - Is workflow intuitive?
     - Are locked decisions clear?
     - Any pain points?
   □ Document feedback & iterate

9. Motion Budget & Token Audit (2 hours)
   □ Audit design tokens:
     - Colors: verify no hardcoded values
     - Typography: verify scale + weights
     - Spacing: verify grid consistency
     - Border radius: verify consistency
     - Shadows: verify depth system
   □ Audit motion budget:
     - Duration limits per interaction
     - Easing curves
     - Frame rate on test devices
   □ Add token linting to CI:
     - eslint-plugin-design-tokens or custom
   □ Create .nezam/core/docs/DESIGN_TOKENS_GUIDE.md:
     - How to use each token category
     - Why each constraint exists
     - Examples in each framework

Output Files:
- .nezam/core/docs/WIREFRAME_RUNBOOK.md
- .nezam/core/docs/DESIGN_TO_CODE.md
- .nezam/core/docs/DESIGN_TOKENS_GUIDE.md
- .nezam/core/reports/WIREFRAME_TESTING_v3.2.md
- .nezam/scripts/validate-block-registry.js
- .nezam/scripts/validate-wireframe-lock.js
- .nezam/templates/components/WireframeExtract.tsx

Success Criteria:
✅ Figma MCP authenticated + tested
✅ Lock→unlock cycle works on 3 project types
✅ Empty blocks resolved
✅ Wireframe runbook written
✅ SCAFFOLD gate enforces lock file
✅ Design-to-code workflow documented
✅ Design team trained + validated
```

---

## Phase 5: Content & Analytics [Content Strategist + Analytics Engineer]

### Prompt for Claude Code

```bash
claude-code --task "NEZAM v3.2 Phase 5: Content Ops & Analytics"
```

### Full Handoff Context

**Objective:** Validate SEO/AEO on real project; set up analytics instrumentation; create content ops workflow

**Tasks (14 hours total):**

```
1. SEO/AEO Strategy Validation (1 hour)
   □ Run /PLAN seo-ia-content for first real project
   □ Document:
     - SEO keyword strategy
     - IA/taxonomy decisions
     - Content map (pages, hierarchy)
     - Validation against platform requirements

2. Content Quality Baseline (1 hour)
   □ Set quality standards:
     - Readability score: Flesch reading ease 60+
     - Word count per page type (blog: 1500-2500, product: 300-500)
     - Links per page (internal 2-5, external 0-2)
     - Heading hierarchy (H1: 1, H2: 2-3, H3: 3-5)
   □ Create .nezam/core/docs/CONTENT_QUALITY_BASELINE.md

3. AEO Optimization (1 hour)
   □ Test content on:
     - ChatGPT (what does it cite?)
     - Claude (what does it recommend?)
     - Gemini (what does it extract?)
     - Perplexity (does it appear?)
   □ Document:
     - Where content appears
     - How it's cited
     - Improvement opportunities
   □ Create AEO guidelines (concise Q&A, bold key terms, etc.)

4. Content Templates (1 hour)
   □ Create templates for each content type:
     - Blog post (title, meta, sections, CTA, SEO checklist)
     - Product page (overview, features, use cases, pricing, CTA)
     - Hub page (cluster definition, child pages, navigation)
     - Landing page (headline, copy, form, CTA, social proof)
   □ Each includes:
     - SEO pre-flight checklist (meta, keywords, links)
     - AEO pre-flight checklist (clarity, Q&A format, citations)
     - Content quality checklist
   □ Save to .nezam/templates/content/

5. Content Operations Workflow (2 hours)
   □ Define workflow: Intake → Outline → Draft → SEO Review → Publish
   □ Create .nezam/core/docs/CONTENT_OPS.md:
     - Step 1: Intake (what, who, when)
     - Step 2: Outline (keywords, structure, flow)
     - Step 3: Draft (first pass, ~2 hours)
     - Step 4: SEO review (keywords, links, meta, AEO)
     - Step 5: Publish (checklist, QA, monitoring)
     - Who does each step
     - Timeline per content type
     - QA checklist before publish
   □ Document roles:
     - Content strategist: outline + QA
     - Writer: draft
     - SEO specialist: review
     - Product: approval
   □ Timeline estimates:
     - Blog: 1 week (outline 1 day, draft 1 day, review 1 day)
     - Product page: 3 days
     - Hub: 1 week
     - Landing: 2 weeks

6. Content Monitoring Setup (1 hour)
   □ Integrate Google Search Console:
     - Track impressions, clicks, ranking per page
     - Set up alerts for ranking drops
   □ Create dashboard:
     - Top 10 pages by traffic
     - New pages waiting for indexing
     - Pages with low CTR (fix meta)
     - Pages with high impressions, low clicks (improve title)
   □ Document: How to read the dashboard, weekly review checklist

7. Analytics Instrumentation (2 hours)
   □ Set up product analytics:
     - Define tracking events (from tracking plan)
     - Instrument event collection
     - Test event delivery
     - Create dashboard (users, engagement, conversions)
   □ Set up error tracking:
     - Sentry or equivalent
     - Configure source maps
     - Test error capture
     - Set up alerts (critical errors → Slack)
   □ Set up performance monitoring:
     - Web Vitals collection
     - Backend latency tracking
     - Database query monitoring
     - Create dashboard

8. Business Metrics Dashboard (1 hour)
   □ Create dashboard with KPIs:
     - Daily active users (DAU)
     - Conversion funnel (signup → paid)
     - Churn rate
     - Net promoter score (NPS)
     - Customer acquisition cost (CAC)
   □ Set up daily reports to stakeholders
   □ Document: How to interpret each metric

9. Observability Runbook (1 hour)
   □ Create .nezam/core/docs/OBSERVABILITY_RUNBOOK.md:
     - How to debug with data
     - How to investigate errors (use Sentry)
     - How to identify perf bottlenecks
     - Escalation procedures
     - Who to contact for each metric

10. Arabic Content Strategy Test (1.5 hours)
    □ Run /PLAN seo-ia-content for Arabic version (if applicable)
    □ Test RTL rendering:
      - Does layout flip correctly?
      - Are fonts readable?
      - Do links work?
    □ Verify Arabic SEO tags:
      - Meta tags in Arabic
      - Hreflang to English version
      - Structured data (schema.org)
    □ Test Arabic AEO:
      - Does ChatGPT cite Arabic content?
      - Is it in Perplexity Arabic search?
    □ Document findings

Output Files:
- .nezam/core/docs/CONTENT_QUALITY_BASELINE.md
- .nezam/core/docs/CONTENT_OPS.md
- .nezam/core/docs/OBSERVABILITY_RUNBOOK.md
- .nezam/templates/content/ (blog, product, hub, landing templates)
- Analytics dashboard (link to tool)
- Google Search Console setup (documented)

Success Criteria:
✅ SEO/AEO strategies documented + tested
✅ Content quality baseline established
✅ Templates created + team-tested
✅ Content ops workflow documented
✅ Monitoring dashboard live
✅ Arabic content validated (if applicable)
```

---

## Phase 6: Integration & Polish [Technical Writer + Team]

### Prompt for Claude Code

```bash
claude-code --task "NEZAM v3.2 Phase 6: Integration, Documentation & Release"
```

### Full Handoff Context

**Objective:** Document all runbooks; train team; perform final QA; get release approval

**Tasks (14 hours total):**

```
1. Documentation Completeness Audit (1 hour)
   □ Verify all runbooks exist:
     - SYNC_RUNBOOK.md ✓
     - CI_FAILURE_GUIDE.md ✓
     - ROLLBACK_RUNBOOK.md ✓
     - SECURITY_RUNBOOK.md ✓
     - WIREFRAME_RUNBOOK.md ✓
     - CONTENT_OPS.md ✓
     - OBSERVABILITY_RUNBOOK.md ✓
   □ Create index: .nezam/core/docs/RUNBOOKS.md
     - Links to all runbooks
     - Use case for each
     - Quick lookup table

2. Troubleshooting Guide Creation (1 hour)
   □ Create .nezam/core/docs/TROUBLESHOOTING.md:
     - 20+ common issues + solutions
     - Organized by system (sync, CI, security, etc.)
     - FAQ format
     - Links to relevant runbooks
     - How to escalate
   □ Include:
     - "Sync drift > 1%" → run pnpm ai:check, see sync runbook
     - "CI gate failing" → see CI failure guide
     - "Secret found" → see security runbook
     - "Wireframe lock empty" → see wireframe runbook
     - etc.

3. Onboarding Guide for New Devs (1 hour)
   □ Create .nezam/core/docs/ONBOARDING.md:
     - Welcome message
     - System requirements (Node version, etc.)
     - First-time setup:
       - Clone repo
       - npx husky install
       - pnpm install
       - pnpm ai:sync
     - Running first feature:
       - /PLAN to scope
       - /DEVELOP to implement
       - /SCAN to check
       - /FIX to remediate
     - Common workflows:
       - How to commit (pre-commit hook runs)
       - How to create PR (CI runs)
       - How to merge (gates must pass)
     - Getting help (who to ask)
     - Links to runbooks + troubleshooting

4. Video Walkthroughs (2 hours)
   □ Record 5-min videos:
     - Video 1: /PLAN workflow (how to create a feature spec)
     - Video 2: /SCAN + /FIX workflow (how to find & fix issues)
     - Video 3: Wireframe session (how to lock & unlock)
     - Video 4: CI debugging (how to fix a failing gate)
   □ Upload to YouTube or internal video platform
   □ Add captions
   □ Link in .nezam/core/wiki/

5. README & Wiki Updates (1 hour)
   □ Update root README.md:
     - v3.2 status (released)
     - System readiness (all >90%)
     - Health score (100/100)
     - What's new in v3.2
     - Quick start link
   □ Update .nezam/core/wiki/Home.md:
     - Navigation to all docs
     - Quick links to runbooks
     - Links to videos
   □ Create `.nezam/core/wiki/Getting-Started.md`:
     - Onboarding overview
     - For developers
     - For designers
     - For writers

6. Release Notes for v3.2 (1 hour)
   □ Create CHANGELOG.md entry:
     - What's new (features, improvements)
     - Breaking changes (if any)
     - Migration guide (if needed)
     - Call to action (upgrade, read runbooks, etc.)

7. Team Training Sessions (2 hours)
   □ Session 1: Sync + state management (30 min)
     - How pnpm ai:sync works
     - What pnpm ai:check reports
     - Common drift issues
     - Q&A
   □ Session 2: CI/CD + release (30 min)
     - How to read CI logs
     - How to fix failing gates
     - How to deploy safely
     - Q&A
   □ Session 3: Design + wireframes (30 min)
     - How /wireframe works
     - How to lock/unlock
     - How to use locked output
     - Q&A
   □ Session 4: Security + analytics (30 min)
     - Security scanning overview
     - Analytics dashboard walkthrough
     - Incident response
     - Q&A

8. Hands-On Labs (1 hour)
   □ Lab 1: Make a feature commit + pass pre-commit
     - Student: make change
     - Student: commit
     - Verify: husky hook runs, sync runs
   □ Lab 2: Create PR + verify CI gates
     - Student: create PR
     - Monitor: CI runs
     - Fix: resolve gate failures
     - Merge: all gates pass
   □ Lab 3: Run wireframe session
     - Student: run /wireframe
     - Student: complete session
     - Verify: wireframes_locked.json generated
   □ Lab 4: Investigate a security finding
     - Scenario: CodeQL finds issue
     - Student: read finding
     - Student: understand risk
     - Student: apply fix

9. Certification Quiz (30 min)
   □ Create quiz.md with 20 questions covering:
     - 5 questions on sync + state
     - 5 questions on CI/CD
     - 5 questions on security
     - 5 questions on design + content
   □ Passing score: 80%+
   □ Re-certify quarterly

10. Final QA & Testing (2 hours)
    □ Comprehensive system test:
      - Run /PLAN on test feature
      - Run /DEVELOP with sample code
      - Run /SCAN on sample code
      - Run /FIX on sample issues
      - Verify all commands work
    □ Load test CI pipeline:
      - Submit 5 simultaneous PRs
      - Verify all gates still pass
      - Check for race conditions
      - Verify no timeout issues
    □ Production readiness checklist:
      - All systems > 90%? ✓
      - All runbooks written? ✓
      - All gates green? ✓
      - Team certified? ✓
      - Documentation complete? ✓
      - Release notes ready? ✓
      - Rollback plan documented? ✓

11. Leadership Sign-Off Meeting (1 hour)
    □ Schedule technical review board meeting
    □ Present:
      - System readiness dashboard
      - Each phase completion
      - Runbooks + documentation
      - Team certification results
    □ Get sign-off from:
      - DevOps Lead
      - Security Officer
      - Design Lead
      - Frontend Lead
      - Engineering Lead
      - Founder/CTO
    □ Create .nezam/core/reports/v3.2_RELEASE_APPROVAL.md:
      - Date
      - Sign-offs (names + roles)
      - Go/no-go decision
      - Release timestamp

Output Files:
- .nezam/core/docs/RUNBOOKS.md (index)
- .nezam/core/docs/TROUBLESHOOTING.md
- .nezam/core/docs/ONBOARDING.md
- .nezam/core/wiki/Getting-Started.md
- README.md (updated)
- CHANGELOG.md (v3.2 entry)
- Videos (4 × 5 min)
- Quiz.md
- .nezam/core/reports/v3.2_RELEASE_APPROVAL.md

Success Criteria:
✅ All runbooks indexed + current
✅ Troubleshooting guide covers 20+ scenarios
✅ Onboarding tested with one new hire
✅ Videos recorded + captioned
✅ README updated with v3.2 features
✅ Team certified (80%+ quiz score)
✅ Leadership sign-off obtained
```

---

## How to Use These Handoffs in Claude Code / Antigravity

### Option 1: Sequential Execution

```bash
# Phase 1
claude-code --task "NEZAM v3.2 Phase 1: Foundation Hardening"
# Wait for completion, verify success criteria

# Phase 2
claude-code --task "NEZAM v3.2 Phase 2: CI/CD Pipeline Full Validation"
# Wait for completion, verify success criteria

# ... etc
```

### Option 2: Parallel Execution (Phases 3, 4, 5)

```bash
# In separate terminals:
claude-code --task "NEZAM v3.2 Phase 3: Security & Compliance Automation" &
claude-code --task "NEZAM v3.2 Phase 4: Wireframe Server Integration" &
claude-code --task "NEZAM v3.2 Phase 5: Content Ops & Analytics" &

# Wait for all to complete
wait
```

### Option 3: Antigravity IDE

```
1. Open .nezam/core/prompts/v3.2-claude-code-handoff.md in Antigravity
2. Click "Phase 1" section
3. Copy full "Full Handoff Context"
4. Paste into Antigravity IDE
5. Run → Agent executes all tasks
6. Review output files
7. Repeat for each phase
```

---

## Success Criteria Summary

By end of v3.2 (2026-06-25):

- ✅ All 10 systems >90% ready
- ✅ Overall health 100/100
- ✅ All 6 runbooks written + team-tested
- ✅ All 7 design gates passing
- ✅ CI/CD pipeline fully automated + tested
- ✅ Security scanning automated
- ✅ Wireframe bridge live + tested
- ✅ Content ops workflow live
- ✅ Analytics dashboards active
- ✅ Team certified + trained
- ✅ Zero critical bugs
- ✅ Leadership sign-off obtained

---

**Document Version:** 1.0  
**Created:** 2026-06-05  
**Status:** Ready for Claude Code / Antigravity execution
